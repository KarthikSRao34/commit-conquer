name: Comment PR Evaluation

on:
  workflow_run:
    workflows: ["Evaluate PR"]
    types: [completed]

permissions:
  actions: read
  contents: read
  issues: write
  pull-requests: write

jobs:
  comment:
    # Only comment for PR-triggered runs from the Evaluate PR workflow.
    if: ${{ github.event.workflow_run.event == 'pull_request' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Resolve PR number from event payload
        id: pr
        run: |
          PR_NUMBER="$(jq -r '.workflow_run.pull_requests[0].number // empty' "$GITHUB_EVENT_PATH")"
          echo "pr_number=$PR_NUMBER" >> "$GITHUB_OUTPUT"

      - name: Download score artifact from evaluate run
        if: ${{ steps.pr.outputs.pr_number != '' }}
        uses: actions/download-artifact@v4
        with:
          name: score-pr-${{ steps.pr.outputs.pr_number }}
          path: eval_results/
          run-id: ${{ github.event.workflow_run.id }}
          github-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Post PR comment
        if: ${{ steps.pr.outputs.pr_number != '' }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_NUMBER: ${{ steps.pr.outputs.pr_number }}
          REPO: ${{ github.repository }}
        run: node eval/post_comment.js
